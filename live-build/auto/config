#!/bin/bash

lb config noauto \
    --distribution ceres \
    --archive-areas "main contrib non-free non-free-firmware" \
    --debian-installer none \
    --bootappend-live "boot=live components quiet splash username=user hostname=spaced console=ttyS0" \
    --binary-image iso-hybrid \
    --iso-application "Spaced Linux 7.26.1" \
    --iso-preparer "Spaced Linux Team" \
    --iso-publisher "Spaced Linux" \
    --iso-volume "SpacedLinux7261" \
    --linux-flavours amd64 \
    --linux-packages linux-image \
    --memtest none \
    --mirror-bootstrap "http://pkgmaster.devuan.org/merged" \
    --mirror-chroot "http://pkgmaster.devuan.org/merged" \
    --mirror-binary "http://pkgmaster.devuan.org/merged" \
    --parent-distribution ceres \
    --parent-mirror-bootstrap "http://pkgmaster.devuan.org/merged" \
    --parent-mirror-chroot "http://pkgmaster.devuan.org/merged" \
    --parent-mirror-binary "http://pkgmaster.devuan.org/merged" \
    --security false \
    --updates false \
    --backports false \
    --firmware-binary false \
    --firmware-chroot false \
    --apt-indices false \
    --apt-source-archives false \
    --apt-recommends false \
    --apt-secure false \
    --cache true \
    --cache-indices false \
    --cache-packages true \
    --cache-stages bootstrap \
    --bootloaders "grub-pc grub-efi" \
    --initramfs live-boot \
    --initsystem sysvinit \
    --mode debian \
    --system live \
    --debootstrap-script sid \
    --verbose \
    --debootstrap-options "--include=systemd-standalone-sysusers,systemd-standalone-tmpfiles --exclude=bootlogd,cron,cron-daemon-common,logrotate"
