# 9.26 desktop stability audit

This is a source/build checkpoint, not a hardware certification. Firmware,
GPU drivers, five physical monitors, 4K hardware and release ISO boots still
require the runtime matrix below before those reports can be closed.

| Issue | Change and evidence | Remaining verification |
| --- | --- | --- |
| #174 | GTK catch-all painted selected text with the ordinary background in three palettes. Removed that selector; added 6px text padding. GTK3/Xvfb computed colors and padding pass all eleven theme files. | Pluma with each syntax scheme and HiDPI scaling. |
| #159 | Screenshot's `window.background.rubberband` lost to a later window override; Caja's `.view.rubberband` also needed an explicit selector. Runtime alpha checks now pass all palettes. | Interactive region selection in screenshot, Caja icon/list views. |
| #165 / #166 | Eye of MATE handles 27 standard image MIME types. Versioned per-user migration repairs browser image handlers and missing text/shell handlers, preserves explicit editor/viewer choices, writes atomic backups and retries malformed configs. | Upgrade representative installed profiles and verify Caja launch behavior. |
| #160 / #161 | About opens `spaced-welcome --page help`; launcher/system-information dialog use the existing silver medallion asset. | Installed menu and Welcome Help tab. |
| #158 | Shift+Print starts MATE area capture; Ctrl+Shift+left-drag starts Compiz capture without Brisk's Super binding. Existing custom shortcut/command slots are retained. | Real keyboard/mouse input; tutorial video remains outside this checkpoint. |
| #176 | DejaVu Sans Mono default and block graph seed; Pango reports zero missing glyphs for box/block/braille sample. Existing btop preferences remain intact. | btop and reported CLI on installed UTF-8 terminal profiles. |
| #162 / #175 | Compiz decorator gets a per-display singleton supervisor; both decorator and WM restart after abnormal exit with capped backoff, respecting clean replacement/logout and X teardown. Behavioral crash tests pass. MATE panel already has upstream AutoRestart. | Kill/recover decorator and Compiz in VM, stress panel under load; original crash stack traces still needed. |
| #178 | Removed `xset dpms force on`, which calls DPMSEnable and overrides disabled power management; removed stale per-output mode/rate replay. Fixture test preserves two positioned 4K outputs, disabled connectors and power state. | Never/off timeout behavior on hardware, GPU/connector/EDID limits. |
| #179 / #180 | Display helper now leaves geometry, refresh rate, rotation, transform and panning to MATE. | Genuine 4K/stacked five-monitor reproductions remain open. |
| #167 | No icon-search restriction reproduced. Upstream MATE scans icon search paths but rejects indexes without `Directories` or with `Hidden=true`. | The reporter's exact extracted icon archive and live Appearance logs; no speculative fork. |

Marco's PID namespace bug has an existing upstream repair:
[PR #786](https://github.com/mate-desktop/marco/pull/786). The two unchanged
source patches are backported to `1.26.2-6+spaced9.26.1`; runtime, development,
debug and source packages built successfully in an isolated Devuan Ceres
chroot, with no host package changes. See [patch notes](../patches/marco/README.md).
Test both sandboxed windows and genuine root-owned windows before declaring
annotation behavior verified. A broad MATE/Compiz fork is not needed for these
source-evidenced distribution fixes.

Primary sources inspected:

- [Compiz decorator lifecycle](https://github.com/compiz-reloaded/compiz/blob/master/plugins/decoration.c)
- [MATE panel AutoRestart](https://github.com/mate-desktop/mate-panel/blob/master/mate-panel/mate-panel.desktop.in)
- [MATE icon index validation](https://github.com/mate-desktop/mate-control-center/blob/master/capplets/common/mate-theme-info.c)
- [MATE screenshot rubberband painting](https://github.com/mate-desktop/mate-utils/blob/master/mate-screenshot/src/screenshot-utils.c)
- [GTK text view padding](https://github.com/GNOME/gtk/blob/gtk-3-24/gtk/gtktextview.c)
- [Eye of MATE MIME capabilities](https://github.com/mate-desktop/eom/blob/master/data/eom.desktop.in.in)
- [btop font/graph requirements](https://github.com/aristocratos/btop#prerequisites-read-this-if-you-are-having-issues)
- [X.Org xset source](https://www.x.org/releases/individual/app/xset-1.2.5.tar.xz) (`xset.c`, force-on branch calls DPMSEnable)

QEMU smoke coverage is implemented in `scripts/vm/qemu/smoke-iso.sh`:
`SPACED_QEMU_FIRMWARE=bios|uefi`, default 2048 MiB/2 CPUs, optional
`SPACED_QEMU_XRES=3840 SPACED_QEMU_YRES=2160`. It uses one standard VGA device
with 64 MiB, copies writable OVMF variables into a private run directory,
checks the live user's Compiz EWMH/decorator windows and `dpkg --audit`, and
validates screenshot pixels and requested dimensions. Evidence consists of
`qemu-runtime.json`, `qemu-live.ppm`, QEMU/serial/guest logs. Mocked launch tests
cover BIOS/UEFI arguments, isolated firmware variables, 4K settings and owned
PID cleanup. **No 9.26 ISO boot or 4K VM success is claimed at this checkpoint.**
