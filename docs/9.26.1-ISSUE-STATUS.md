# Spaced Linux 9.26.1 issue status

Audit date: 2026-09-10. This covers all 21 issues open in
[crhy/spaced](https://github.com/crhy/spaced/issues) at the time 9.26.1 was
prepared. It follows the tracker policy established for 9.26: a tested source
change is not the same thing as a fix confirmed on the affected machine, and no
issue is closed here.

The latest published image is **9.26**. 9.26.1 is a maintenance release whose
source changes are described below; the download links in `README.md` and on
the website move to 9.26.1 only after that ISO and its checksum are published
and verified.

## Fixed in source for 9.26.1

| Issue | Change | Evidence | Still required before closing |
| --- | --- | --- | --- |
| [#160](https://github.com/crhy/spaced/issues/160), [#181](https://github.com/crhy/spaced/issues/181) About Spaced Linux | The menu entry opens `spaced-system-info` again instead of Welcome's help page. The dialog leads with the fancy medallion, states the installed release from `VERSION_ID`, and links to spacedlinux.com and the donation page; hardware details moved into a collapsed expander. Welcome's offline help kept its own `Spaced Linux Help` launcher so [#161](https://github.com/crhy/spaced/issues/161) stays fixed. | `scripts/check.sh` asserts the launcher target, icon, version source and both links, and that the help launcher still exists. | Visual check of the installed menu entry and dialog at normal and high DPI. |
| [#184](https://github.com/crhy/spaced/issues/184) Gigolo desktop icons | Caja stores an explicit position for every desktop icon, including gvfs network mounts. Positions saved against a monitor layout that no longer exists put those icons outside the visible area — which is why the reporter saw them reappear only once a fifth display extended the desktop ([#180](https://github.com/crhy/spaced/issues/180)). `spaced-desktop-icon-repair` clears only unreachable positions, at login and on every monitor-layout change, so Caja re-places those icons; `volumes-visible` is now set explicitly. | Four behavioural tests in `tests/test_desktop_reliability.py` cover off-screen clearing, an on-screen desktop being left untouched, an unreadable layout doing nothing, and the gvfs metadata path. | Connect the reporter's shares through Gigolo on the five-monitor machine and confirm the icons appear, and that a deliberately arranged desktop survives a reboot. |
| [#178](https://github.com/crhy/spaced/issues/178) Display "Never" ignored | MATE writes `0` for Never and then stops programming a timeout, which leaves the X server's own DPMS and screen-blank defaults in force — so the monitors still powered down after ten minutes. The session now issues `xset -dpms`/`s off`/`s noblank` when Never is selected, hands DPMS back to mate-power-manager when a real timeout is chosen, and re-applies on unlock and whenever the setting changes. | Behavioural tests for the Never case, the real-timeout case, and a system without the MATE power schema. | Leave the OptiPlex idle with Never selected, on the reporter's hardware, and confirm the monitors stay on. |
| [#181](https://github.com/crhy/spaced/issues/181) Appearance Preferences crash | Every theme change ran a full desktop-extras pass: an rsync of eleven theme trees into the Flatpak export directory, two `flatpak override` calls, a Python interpreter to read the theme table, plus panel and dock work — all of it concurrently with the previous change. Changes are now coalesced over a settle window, switches are serialised under one lock, the theme table is read once per session, and the export is rebuilt only when the installed themes actually change. | A behavioural test drives the real settle loop: eight rapid changes collapse to one switch, and a later separate choice still applies. | Switch repeatedly through all eleven themes on the Ryzen 3 laptop that crashed, watching CPU. |
| [#182](https://github.com/crhy/spaced/issues/182) Pluma not retained for scripts | `text/x-python`, `text/x-python3`, the `text/x-sh`/`application/x-sh` aliases and the Perl types were never declared, so those launches fell through to whatever the shared MIME database guessed. They are now declared system-wide, in `/etc/skel`, and in the per-account migration, which still preserves an explicitly chosen editor. Double-clicking an executable script opens it in the editor rather than prompting to run it. | A migration test asserts a user's existing choice survives while every script type gains the Pluma default. | Open `.sh` and `.py` files from Caja on a fresh account and on an upgraded account. |
| [#181](https://github.com/crhy/spaced/issues/181) uBlock Origin in Brave | A managed policy enables Manifest V2 availability and installs uBlock Origin (`jcokkipkhhgiakinbnnplhkdbjbgcgpe`, the build Brave itself installs for MV2) with `normal_installed`, so it is present by default but can still be disabled. | The Brave Flatpak's launcher symlinks `/etc/brave/policies/{managed,recommended,enrollment}/*.json` from the host into its sandbox, so the shipped file reaches Flatpak Brave as well as a native one; `scripts/check.sh` asserts the policy contents. | Confirm on an installed system that `brave://policy` lists the policy and the extension is active in a new profile. |
| [#181](https://github.com/crhy/spaced/issues/181) Empty Explore during sync | `BzFlathubPage` switched to its content view as soon as the Flathub state object existed — long before the catalog arrived — so Explore sat empty for the whole sync. It now stays on the updating view, which also shows the current sync step, until there is something to browse, and a later manual re-sync no longer blanks a catalog the user is reading. | The SpacedBazaar Flatpak builds clean with the change. | Start SpacedBazaar on a fresh installation and confirm the updating view persists until apps appear. |
| [#181](https://github.com/crhy/spaced/issues/181) Unreadable search titles | The search results grid gave each rich tile roughly 300px, and an ellipsizing label will happily shrink to a bare "…", which is why results showed only an ellipsis and the verified checkmark. The grid was widened and the title given a minimum width. | Builds clean. | Search on the desktop that showed the problem, including at the 1.2× text scaling Spaced sets by default. |

## Reported fixed by the reporter, awaiting closure

| Issue | Position |
| --- | --- |
| [#178](https://github.com/crhy/spaced/issues/178), [#180](https://github.com/crhy/spaced/issues/180), [#166](https://github.com/crhy/spaced/issues/166) | The reporter has said each of these threads can be closed. Their remaining concrete symptoms — display power policy and desktop icons — are the 9.26.1 changes above; the resolution and GPU limits are hardware, not defects. |

## Not fixed in 9.26.1

| Issue | Position |
| --- | --- |
| [#164](https://github.com/crhy/spaced/issues/164) GIMP scale-dialog crash | Still present. The shipped Flatpak is GIMP 3.2.4, the current Flathub build, so there is no newer release to move to and no distribution-side patch. This needs a reproduction with a crash trace and an upstream report; changing the unit in the Scale Image dialog is not reachable from a script. |
| [#185](https://github.com/crhy/spaced/issues/185) Live video wallpaper | Not addressed. The reporter describes choppy window dragging only while the wallpaper is running, which is a compositing cost question rather than a defect with a known cause. |
| [#183](https://github.com/crhy/spaced/issues/183) Fork Compiz and MATE | Not started. Forking and tuning two upstream desktops is not point-release work; it needs its own plan, maintenance commitment and test matrix. |
| [#158](https://github.com/crhy/spaced/issues/158) Compiz tutorial | The non-conflicting bindings shipped in 9.26. The requested video showing the keys alongside the effects has not been produced. |
| [#181](https://github.com/crhy/spaced/issues/181) Catalog screenshots | Dark-theme captures of SpacedBazaar, Brave, LibreOffice and Audacious, and a Voice2Text AI screenshot, all require running those applications on a Spaced desktop and capturing them. They cannot be produced from source. rhYciv is present in the local catalog; confirm whether the published catalog has caught up. Scum With Friends has no release to publish yet. |
| [#181](https://github.com/crhy/spaced/issues/181) Determinate sync progress | The updating view now shows the live sync step, but a true percentage needs the refresh worker to report progress into the existing, currently unused `busy-progress` property. |
| [#148](https://github.com/crhy/spaced/issues/148) Monthly builds | Workflow and targets exist; a recorded successful scheduled run and install matrix are still outstanding. |
| [#140](https://github.com/crhy/spaced/issues/140) Help library | Content exists offline and on the site; publishing and a walk-through on real devices remain. |
| [#157](https://github.com/crhy/spaced/issues/157), [#147](https://github.com/crhy/spaced/issues/147), [#82](https://github.com/crhy/spaced/issues/82), [#80](https://github.com/crhy/spaced/issues/80), [#53](https://github.com/crhy/spaced/issues/53), [#52](https://github.com/crhy/spaced/issues/52), [#50](https://github.com/crhy/spaced/issues/50), [#9](https://github.com/crhy/spaced/issues/9) | Deferred roadmap features, unchanged from the 9.26 audit. Each is a separate product or subsystem rather than a bug. |

## Session efficiency work

These are not tracker issues; they came out of reading every session helper
that runs on a Spaced desktop.

- `spaced-audio-restore` saved state on every PulseAudio sink event, spawning
  three `pactl` processes each time. Dragging the volume slider therefore
  produced a burst of process spawns. Events are now coalesced into one save.
- `spaced-enable-flatpak-remotes` retried every two seconds for three minutes.
  On a machine that is simply offline that is ninety rounds of several Flatpak
  processes for no possible gain. It now backs off to a thirty-second interval.
- `spaced-theme-monitor` started a Python interpreter per theme change to read
  a table that never changes during a session. It is read once at startup.
