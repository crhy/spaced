---
backend: apt

# The live image carries debugging and installation tools that should not be
# left behind on the lean installed system.
operations:
  - remove:
      - live-boot
      - live-config
      - live-config-sysvinit
      - openssh-server
      - calamares
      - calamares-settings-debian
      - squashfs-tools
