# The live image uses nouveau/Mesa by default. Proprietary NVIDIA modules are
# intentionally not baked into the universal ISO; install them after setup
# with Spaced NVIDIA Driver Installer.
blacklist nvidia
blacklist nvidia_drm
blacklist nvidia_modeset
blacklist nvidia_uvm
